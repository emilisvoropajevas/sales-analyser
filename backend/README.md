#Backend
The backend is setup with FastAPI and SQLite for local storage of saved reports
The backend functions as the brain of the system and tool, it cleans, formats and sends incoming csv after saving the report to a local database
